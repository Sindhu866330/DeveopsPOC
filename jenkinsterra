pipeline {
    agent any
	tools {
  terraform 'terraform'
}
    stages {
        stage('build') {
            steps {
                echo "Hello World!"
                bat "echo Hello from the shell"
                bat "hostname"
                bat "uptime"
            }
        }
    }
}
